# graphify: caché regenerable (graphify update .)
graphify-out/cache/

# caché de runtime de widgets
Widgets/Bar/.cache

# estado local de esta máquina: config de usuario (los defaults viven en
# Commons/Config.qml, así que sin ella el shell arranca igual) y la sesión
# de Instagram, que son credenciales — el directorio entero fuera de git.
Config/

# editor / OS
*~
*.swp
.DS_Store

CLAUDE.md

graphify-out

Workspace

Comparativa
